package dev.hindsight.benchmark;

/** Where the measured code lives, in one place so the agent and the benchmark cannot disagree. */
final class Workloads {

    static final String PACKAGE = "sample.workload";

    private Workloads() {
    }
}
