spring.application.name=hindsight-demo

# The playground compiles and runs whatever is pasted into it. That is fine on the machine you
# are sitting at and indefensible anywhere else, so this binds to loopback and stays there.
server.address=127.0.0.1
